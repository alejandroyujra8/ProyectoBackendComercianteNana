apiVersion: apps/v1
kind: Deployment
metadata:
  name: nana-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nana-frontend
  template:
    metadata:
      labels:
        app: nana-frontend
    spec:
      containers:
        - name: frontend
          image: nana-frontend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nana-frontend
spec:
  type: NodePort
  selector:
    app: nana-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
